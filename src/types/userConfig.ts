
export interface userBackgroundColor {
  btn: string;
  color: string;
  emptyBtn: string;
  hexa: string;
  name: string;
}

export interface userColorInterface extends userBackgroundColor {
  text: string;
  textName: string
}
